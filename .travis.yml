sudo: false
#addons:
#  apt:
#    packages:
#      - oracle-java8-installer

matrix:
    include:
    - os: linux
      jdk:  oraclejdk8
      install: true
    - os: linux
      jdk:  oraclejdk9
      install: true
    - os: osx
      osx_image: xcode9.2
      install: brew tap caskroom/cask && brew cask info java

language: java


before_script:
  - sh ./gradlew --version
  
script:
    - sh ./gradlew clean test
    
#after_success:
#  - TERM=dumb ./gradlew test jacocoTestReport jacocoRootReport coveralls -S
